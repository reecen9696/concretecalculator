export default function App() {
  return (
    <div className="min-h-screen bg-surface-base text-ink p-6 font-sans">
      <p className="text-sm text-ink-muted">Scaffold — UI lands in step (d).</p>
    </div>
  );
}
